{
  "name": "tea-cup-package",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for tea-cup libraries",
  "scripts": {
    "build": "pnpm --filter \"*\" build",
    "check": "pnpm --filter \"*\" check",
    "lint": "pnpm --filter \"*\" lint",
    "format": "pnpm --filter \"*\" format"
  }
}
